<div class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
		<div class="container">

      <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
 
      <!-- Be sure to leave the brand out there if you want it shown -->
      <%= link_to "One Month Rails", root_path, class: "brand" %>
 
      <!-- Everything you want hidden at 940px or less, place within here -->
      <div class="nav-collapse collapse">
        <ul class="nav">
  				<li class="<%= 'active' if current_page?(root_path) %>">
    				<%= link_to "<i class=\"icon-home\"></i> Home".html_safe, root_path %>
  				</li>
  				<li class="<%= 'active' if current_page?(about_path) %>">
  					<%= link_to "<i class=\"icon-bullhorn\"></i> About".html_safe, about_path %>
  				</li>
				</ul>
				<ul class="nav pull-right">
  				<% if user_signed_in? %>
            <li class="<%= 'active' if current_page?(edit_user_registration_path) %>">
              <%= link_to "<i class=\"icon-cogs\"></i> Account".html_safe, edit_user_registration_path %>
            </li>
            <li>
              <%= link_to "<i class=\"icon-unlock\"></i> Logout".html_safe, destroy_user_session_path, method: :delete %>
            </li>
          <% else %>
            <li>
      				<%= link_to "<i class=\"icon-lock\"></i> Login".html_safe, new_user_session_path %>
    				</li>
          <% end %>
				</ul>
      </div>

    </div>
  </div>
</div>